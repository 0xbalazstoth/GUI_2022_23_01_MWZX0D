<Window x:Class="Gunner.InventoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Gunner"
        mc:Ignorable="d"
        Title="Inventory" Height="300" Width="400" WindowStyle="None" WindowStartupLocation="CenterScreen">
    <Grid>
        <ListBox x:Name="lstBoxInventory">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Image Margin="3" Source="pack://application:,,,/Assets/Textures/health_potion.png"></Image>
                        <Label Margin="3" Foreground="Black" Content="{Binding Value.ItemType}" />
                        <Label Margin="3" Foreground="Black" x:Name="lblQuantity"></Label>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
